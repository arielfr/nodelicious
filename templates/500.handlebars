{{#extend "main"}}
    {{#content "header" mode="append"}}
        <link rel="stylesheet" href="/css/errors.css">
    {{/content}}
    {{#content "body"}}
        {{#extend "modules/navbar"}}{{/extend}}
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <h1>500</h1>
                    <p>An error ocurred</p>
                </div>
            </div>
        </div>
    {{/content}}
    {{#content "scripts" mode="append"}}
        <script type="application/javascript" src="/js/errors.js"></script>
    {{/content}}
{{/extend}}