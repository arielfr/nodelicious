<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#n-navbar" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">{{navbar.title}}</a>
        </div>

        <div class="collapse navbar-collapse" id="n-navbar">
            <ul class="nav navbar-nav">
                <li>
                    <a href="#">TAG CLOUD</a>
                </li>
                <li>
                    <a href="#">PICTURE WALL</a>
                </li>
                <li>
                    <a href="#">DAILY</a>
                </li>
                <li>
                    <a href="#">SEARCH</a>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {{#if isLogged}}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            Hola {{user.firstname}} <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>
                                <a href="/logout">Logout</a>
                            </li>
                        </ul>
                    </li>
                {{else}}
                    <li class="{{#if navbar.login}}active{{/if}}">
                        <a href="/login">Log In <i class="material-icons">account_circle</i></a>
                    </li>
                    <li class="{{#if navbar.register}}active{{/if}}">
                        <a href="/register">Register</a>
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>