<div class="container footer">
    <div class="row">
        <div class="col-sm-1 col-md-3"></div>
        <div class="col-xs-12 col-sm-9 col-md-6 text-center">
            {{#if total}}
                <div class="total">
                    <span>{{total}} links</span>
                </div>
            {{/if}}
            <span>Nodelicious - Superfast online bookmarking with Google Material Design</span>
        </div>
        <div class="col-sm-1 col-md-3"></div>
    </div>
</div>